import { z } from 'zod';
export const lessonSchema=z.object({id:z.string().min(1).max(90),title:z.string().min(1).max(90),concept:z.string().max(45),task:z.string().min(1).max(250),level:z.number().int().min(1).max(3),before:z.string().max(1400),after:z.string().max(800),choices:z.array(z.object({text:z.string().max(250),grade:z.enum(['right','okay','wrong']),why:z.string().max(400)})).min(2).max(4),pieces:z.array(z.object({id:z.string().min(1).max(20),code:z.string().max(300).refine(x=>!x.includes('\n')),purpose:z.string().max(220),indent:z.number().int().min(0).max(4)})).min(2).max(7),output:z.string().max(1500),explanation:z.string().max(500),alternatives:z.array(z.object({name:z.string().max(40),code:z.string().max(2200),why:z.string().max(350)})).min(1).max(5)}).superRefine((v,c)=>{if(new Set(v.pieces.map(p=>p.id)).size!==v.pieces.length)c.addIssue({code:'custom',message:'Piece IDs must be unique'});if(!v.choices.some(x=>x.grade==='right'))c.addIssue({code:'custom',message:'A puzzle needs a correct answer'});});
